{
  "name": "profdocs-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack professional document intelligence and context-engine platform built with Angular, NestJS, PostgreSQL, Docker and Documentation as Code.",
  "license": "MIT",
  "author": "Gabriel Bory Prevez",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --from HEAD~1 --to HEAD --verbose",
    "dev:web": "pnpm --filter web start",
    "dev:api": "pnpm --filter api start:dev",
    "dev:docs": "pnpm --filter docs dev",
    "build": "pnpm -r build",
    "build:web": "pnpm build:shared && pnpm --filter web build",
    "build:api": "pnpm --filter api build",
    "build:docs": "pnpm --filter docs build",
    "e2e": "playwright test -c apps/web/e2e/playwright.config.ts",
    "e2e:ui": "playwright test -c apps/web/e2e/playwright.config.ts --ui",
    "e2e:headed": "playwright test -c apps/web/e2e/playwright.config.ts --headed",
    "e2e:manual": "playwright test -c apps/web/e2e/playwright.config.ts --project=manual-chromium-desktop --project=manual-mobile-chrome",
    "e2e:report": "playwright show-report apps/web/e2e/playwright-report",
    "docs:manual:evidence": "pnpm e2e:manual",
    "docs:manual:build": "pnpm docs:manual:evidence && pnpm build:docs",
    "docs:manual:export": "node scripts/docs/export-user-manual.mjs",
    "docs:manual:export:full": "pnpm docs:manual:build && pnpm docs:manual:export && pnpm build:docs",
    "build:shared": "pnpm --filter @profdocs-ai/shared build",
    "typecheck:shared": "pnpm --filter @profdocs-ai/shared typecheck",
    "test:shared": "pnpm --filter @profdocs-ai/shared test",
    "e2e:context-engine": "playwright test -c apps/web/e2e/playwright.config.ts apps/web/e2e/tests/manual/context-engine-preview.spec.ts --project=manual-chromium-desktop --project=manual-mobile-chrome"
  },
  "keywords": [
    "angular",
    "nestjs",
    "typescript",
    "postgresql",
    "pgvector",
    "rag",
    "ai",
    "docker",
    "monorepo",
    "portfolio",
    "context-engine",
    "skill-registry",
    "prompt-orchestrator",
    "documentation-as-code"
  ],
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "@playwright/test": "^1.61.0",
    "docx": "^9.7.1",
    "husky": "^9.1.7"
  }
}
